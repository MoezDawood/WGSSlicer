version: '3.8'

services:
  wgs-slicer:
    build: .
    ports:
      - "8501:8501"
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      - ./SavedSlicerFilterSet_*.json:/app/SavedSlicerFilterSet_*.json
    restart: unless-stopped

